import SwiftUI

extension Color {
    /// Brand accent — used on return key, dotted-key indicator, and the
    /// upcoming long-press popover row highlights.
    static let ipaAccent = Color(red: 0.40, green: 0.67, blue: 1.0)
}
